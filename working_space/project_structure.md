arxiv-agent/
├── .env                  ← API key lives here
├── models.py             ← all Pydantic models
├── tools.py              ← LangChain tool definition
├── agent.py              ← agent setup and execution
└── main.py               ← entry point